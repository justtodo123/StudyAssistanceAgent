# M8 `draft-0.10` P0 独立复核工作单

- 用途：供被指定的独立 reviewer 逐项核验并自行裁定 `draft-0.10` 的 P0
- 日期：2026-09-13
- 指定 reviewer：`justtodo123`（仓库负责人），角色 `independent-reviewer`
- 记录性质：**`REVIEW_WORKSHEET`**——不是门禁记录，**不预置结论**，不构成任何授权

## 0. 使用方式

本工作单由**起草方**编写，因此其中每一栏都属**待核验的转述**，不属证据。reviewer 应：

1. 对每个 `[ ]` 项**自行执行**命令或独立计算，不采信本单给出的期望值；
2. 期望值列只用于发现"你以为对、实际不对"的分歧——**分歧一旦出现，以你实测为准**；
3. 在 §3 的裁定表中自行填写结论；
4. 产出的记录**不改写任何既有记录**。

起草方未参与本工作单的任何裁定，其自检结论（`tools/*.py` 的 `PASS`）在本轮中只作**参考输入**。

## 0.5 行号口径

本工作单与材料说明中的所有行号，均为 **`draft-0.10` 自身**的行号。`draft-0.10` 较 `draft-0.9` 在前部
新增了 A1 内容，两版行号整体相差约 41 行；在 `draft-0.9` 上取得的行号**不得**直接套用到 `draft-0.10`。
若你发现引用的行号与实际内容不符，以你实测为准，并计入发现。

## 1. 被审字节

- [ ] **1.1** 确认被审对象路径为 `docs/plans/references/m8-active-execution-protocol-draft-0.10.md`
- [ ] **1.2** 实测字节数与 SHA-256

```
python -c "import hashlib;b=open(r'docs/plans/references/m8-active-execution-protocol-draft-0.10.md','rb').read();print(len(b),hashlib.sha256(b).hexdigest(),'CRLF' if b'\r\n' in b else 'LF')"
```

| 项 | 起草方转述 | 你实测 | 一致？ |
| --- | --- | --- | --- |
| 字节数 | 186129 | | |
| SHA-256 | `b5bc5088486079971eba28efe5cd2d7ed90c73a1359a87d9bca4722c473caa39` | | |
| 行尾 | 纯 LF | | |

- [ ] **1.3** 确认工作区字节 == 仓库字节（`git cat-file -p HEAD:<path>` 对照）

> 该对照在 D1a 生效后才成立；D1a 之前 `draft-0.9` 正是因二者不等而使记录在 LF 环境失效。

## 2. 逐项核验

### 2.1 独立性（先决项）

- [ ] **2.1.1** 确认本人未参与 `draft-0.10` 的起草：未编写协议文本、未编写修订或核验脚本、未产出本材料说明。
- [ ] **2.1.2** 确认"批准修订授权"与"审查协议文本"是两项不同行为。
- [ ] **2.1.3** 对照 `draft-0.5` 与 `draft-0.9` 的 P0 先例，确认同一安排是否被本仓库接受。
- [ ] **2.1.4** 若判定独立性**不成立**，应直接记 `P0_NOT_ACCEPTED` 并停止后续项——这是 `draft-0.9`
  `-r01` 的既有先例，本仓库接受该结果。

### 2.2 授权范围（四项是否越界）

- [ ] **2.2.1** 逐项比对 `draft-0.10` 的改动与授权记录 §1 表列方案（A1 / B1 / C2a / D1a），确认**不多不少**。
- [ ] **2.2.2** 核查 A1 的 §1.1 约束（第 116 行起）：是否**只**新增 P2 专用规则，**未**改动 `allowed_system_streams`
  的**类型定义**？若必然要求改类型定义，则 A1 已越界，应记 `P0_NOT_ACCEPTED`。

```
git diff HEAD~1 HEAD -- docs/plans/references/m8-active-execution-protocol-draft-0.10.md | head -100
```

建议改用与 `draft-0.9` 的对照，因 `draft-0.10` 是新文件：

```
git diff --no-index docs/plans/references/m8-active-execution-protocol-draft-0.9.md docs/plans/references/m8-active-execution-protocol-draft-0.10.md
```

- [ ] **2.2.3** 核查授权记录 §4 的**明确禁止**清单，逐条确认未被触碰。

### 2.3 A1：P2 阶段容许表

- [ ] **2.3.1** 独立复核**缺陷本体**是否真实存在：`allowed_system_streams` 是否**确实**属于 P5 的
  `sa.m8.child-allowlist.v1.payload`？P2 是否确实早于 P5？若该前置关系不成立，A1 的前提即不成立。
- [ ] **2.3.2** 核对新增的 `BINDING_STREAM_ALLOWLIST`：其每一行是否都指明 `scope`、`stream`、
  `observation` 三要素？判定方式是否与 §7 既有 per-open 语义一致？
- [ ] **2.3.3** 核对 §7 阶段选择规则（第 990–997 行）是否**互斥且穷尽**、且**机械可判**（不依赖人类判断）。
- [ ] **2.3.4** **重点**：判断三条取值 `absent-if-empty` 在 P2 语义下是否成立。起草方在此项上有过一次
  误选（初选 `present-and-reverified`，后改为 `absent-if-empty`），理由是三 scope 在 `C:` 卷上不暴露
  `sguard`。请独立判断该理由是否成立，而非采信。
- [ ] **2.3.5** 确认 A1 **未**改动 P5 的 `child_allowlist` 语义（授权 §3 冻结项）。

### 2.4 B1：类型标注

- [ ] **2.4.1** 读取第 85–86 行，确认 `ID` 不含点号、`SCHEMA_ID` 含点号且为 `ID` 超集。
- [ ] **2.4.2** 实测该字段 13 个取值：是否全部匹配 `SCHEMA_ID`、全部不匹配 `ID`？
- [ ] **2.4.3** 确认两处标注（第 198、377 行）已改为 `SCHEMA_ID`，且**取值未被改动**。
- [ ] **2.4.4** **判断**："改标注"与"改取值"两种修法都能消除矛盾，脚本只能证明矛盾存在、不能判定哪侧
  应改。你是否接受"改标注"这一选择？（本项目采用 B1；B2 改取值已被否决。）
- [ ] **2.4.5** 确认第 85–86 行的**类型定义本体**未被改动（授权 §3 冻结项）。

### 2.5 C2a：排序

- [ ] **2.5.1** 读取第 167 行 `order=value` 定义，确认其对 `SCHEMA_ID` 等按 UTF-8 bytes 排序。
- [ ] **2.5.2** 独立算出该定义下的顺序，确认是否为 `v1, v10, v11, …, v2, …`。
- [ ] **2.5.3** 对照 `draft-0.5`/`draft-0.9` 既有记录，确认该字段实际排列为数字序 → **两者确实冲突**。
- [ ] **2.5.4** 核对新 schema `A<{id:SCHEMA_ID,ordinal:INT[0,12]};13..13;order=key(ordinal);unique=key(id)>`
  两处是否一致（第 198、377 行）；`order=key(...)` 机制是否**协议已有**，未新增 comparator 种类。
- [ ] **2.5.5** 确认 `order=value` 的比较器定义本体未被改动；核查其出现次数（转述：29 处 / 27 行）。

### 2.6 D1a：行尾

- [ ] **2.6.1** `git check-attr text eol -- <两个协议路径>`，确认规则为 `text: set` / `eol: lf`。
- [ ] **2.6.2** 确认规则为**逐文件**而非通配，且 `draft-0.5` **未**被纳入。
- [ ] **2.6.3** 实测 `draft-0.9` 为 181209 bytes / `6ccebc47…` / 纯 LF，且工作区与仓库一致。
- [ ] **2.6.4** 确认 `draft-0.5` 未被本次修订触及：工作区 171830 / `ac907b83…`（CRLF）、
  仓库 170550 / `4ab35a66…`（LF），差 1280 = CRLF 对数。
- [ ] **2.6.5** **重点**：起草方已披露 `draft-0.9` 在本轮中**曾一度被就地改写**（曾变为 184174 bytes），
  随后恢复。请确认当前字节与仓库历史提交一致，并判断该过程是否影响你对"逐版独立"惯例的信任。

### 2.7 冻结不变量（授权 §3）

- [ ] **2.7.1** `sa.m8.status-map.v1.payload`（第 1110–1112 行）：行数、每行取值、行序、两项 72 基数是否逐字不变？
- [ ] **2.7.2** `TECH_GATE_ID` 闭合枚举（第 137 行）是否恰为 **26 项**？第 832、848 行是否仍为 `26..26`？
- [ ] **2.7.3** §7 的 per-open stream 复验语义（第 976–1008 行）是否仍为原判定流程？
- [ ] **2.7.4** 其余 schema、枚举、谓词、数量、deadline 与状态机转移是否保持？

### 2.8 全链复核

- [ ] **2.8.1** 独立重跑全部校验器并记录各自结果（**注意**：这些脚本由起草方编写，其 `PASS` 不构成独立性证据）：

```
for s in m8_validate_p1_materials m8_validate_p0_r02 m8_verify_protocol_inconsistencies m8_verify_proposal_claims m8_verify_authorization_claims; do python tools/$s.py >/dev/null 2>&1 && echo "PASS $s" || echo "FAIL $s"; done
```

- [ ] **2.8.2** 确认 `draft-0.9` 的 **5 条记录仍绑 `162c9047…`**（修订前摘要），此为负责人决定的待办，
  重算留待本次 P0 之后。该项**不应**被当作本次审查的缺陷。
- [ ] **2.8.3** 确认 `draft-0.5` 的 3 条记录仍绑 `ac907b83…`，未被触及。

## 3. 裁定表（由 reviewer 亲自填写）

| 项 | 你的裁定 | 依据（写入记录） |
| --- | --- | --- |
| 独立性是否成立 | | |
| A1 是否在授权范围内 | | |
| B1 是否在授权范围内 | | |
| C2a 是否在授权范围内 | | |
| D1a 是否在授权范围内 | | |
| 冻结不变量是否全部保持 | | |
| 有无本单未列出的发现 | | |
| **P0 决定** | | |

**P0 决定**只能取二者之一：

- `P0_TECHNICAL_SCOPE_WORDING_ACCEPTED_ONLY` —— 仅表示技术文字被接受，不产生执行权限、不解除任何禁令
- `P0_NOT_ACCEPTED` —— 须写明具体缺陷

## 4. 本工作单明确不做的事

- 不预置 `P0` 结论，不建议倾向；
- 不代替 reviewer 作任何判断；
- 不授权任何执行、binding、准入或后端选择；
- 不改写任何既有记录；
- 不代表起草方对 `draft-0.10` 技术文字的自我评价。

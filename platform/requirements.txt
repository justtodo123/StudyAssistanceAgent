fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
# Official first-party Anthropic Messages API adapter for M6b preview.
anthropic>=1.2.0,<2

# M7 user-source parser matrix. Runtime rejects a missing or non-exact parser
# version instead of selecting a fallback parser.
markdown-it-py==4.0.0
pypdf==6.0.0
python-pptx==1.0.2
python-docx==1.2.0

# M7 user-source FTS5 tokenizer. Runtime rejects a missing or non-exact jieba
# version instead of falling back to unicode61, whitespace, or vector-only.
jieba==0.42.1

# 可选：本地向量检索（安装后自动启用，会一同安装 torch）
# sentence-transformers>=2.7

[pytest]
# 测试发现路径
testpaths = tests platform/tests

# 自定义 markers（用于按阶段筛选测试）
markers =
    m3a: M3a 向量库迁移测试
    m3b: M3b 可观测性测试
    m3c: M3c 面经库测试
    m3d: M3d 方法论沉淀测试
    slow: 慢速测试（如 RAG 评测）

# 默认选项
addopts = -v --tb=short

# 忽略的目录
norecursedirs = .venv __pycache__ .git .claude
